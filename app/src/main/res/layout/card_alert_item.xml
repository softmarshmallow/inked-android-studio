<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="138dp">

    <TextView
        android:id="@+id/AlertTitleTextView"
        android:layout_width="235dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:layout_marginLeft="8dp"
        android:layout_marginTop="28dp"
        android:maxLines="1"
        android:text="@string/dummy_title_lorem"
        app:layout_constraintStart_toEndOf="@+id/isReadedIndicatorImage"
        app:layout_constraintTop_toTopOf="parent"/>

    <TextView
        android:id="@+id/AlertContentTextView"
        android:layout_width="336dp"
        android:layout_height="67dp"
        android:layout_marginStart="8dp"
        android:layout_marginLeft="8dp"
        android:layout_marginTop="8dp"
        android:layout_marginEnd="20dp"
        android:layout_marginRight="20dp"
        android:layout_marginBottom="8dp"
        android:maxLines="5"
        android:text="@string/dummy_content_lorem"
        app:layout_constraintBottom_toBottomOf="@+id/view"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintHorizontal_bias="0.0"
        app:layout_constraintStart_toEndOf="@+id/isReadedIndicatorImage"
        app:layout_constraintTop_toBottomOf="@+id/AlertTitleTextView"
        app:layout_constraintVertical_bias="0.0"/>

    <ImageView
        android:id="@+id/isReadedIndicatorImage"
        android:layout_width="8dp"
        android:layout_height="8dp"
        android:layout_marginStart="10dp"
        android:layout_marginLeft="10dp"
        android:layout_marginTop="33dp"
        android:src="@drawable/oval"
        android:tint="@color/colorNotificationTint"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"/>

    <View
        android:id="@+id/view"
        android:layout_width="match_parent"
        android:layout_height="0.5dp"
        android:background="@color/colorDivider"
        app:layout_constraintBottom_toBottomOf="parent"/>

</android.support.constraint.ConstraintLayout>